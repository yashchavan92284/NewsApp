name: news_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  google_fonts: ^6.2.1
  flutter_spinkit: ^5.2.1
  http: ^1.2.1
  cached_network_image:
  intl: ^0.19.0


flutter:
  uses-material-design: true
  assets:
    - images/

